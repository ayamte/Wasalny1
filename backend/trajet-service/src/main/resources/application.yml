spring:
  application:
    name: trajet-service
  datasource:
    url: jdbc:postgresql://localhost:4081/trajet_db
    username: wasalny_user
    password: wasalny_password
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8081

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
